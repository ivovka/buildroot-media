--- nvram-wakeup-1.1/Makefile.orig	2011-11-14 09:09:07.000000000 +0400
+++ nvram-wakeup-1.1/Makefile	2011-11-14 09:09:50.000000000 +0400
@@ -6,7 +6,7 @@
 
 
 # where do you want the program to be installed?
-prefix   = /usr/local
+prefix   ?= /usr/local
 BINDIR   = ${prefix}/bin
 SBINDIR  = ${prefix}/sbin
 MANDIR   = ${prefix}/man
@@ -39,7 +39,7 @@
 TMPDIR = /tmp
 VERSION = $(shell sed -ne '/define VERSION/s/^.*"\(.*\)".*$$/\1/p' nvram-wakeup.h)
 
-CC = gcc
+CC ?= gcc
 DEFS = -D_GNU_SOURCE
 CFLAGS = -O2 -Wall -Wstrict-prototypes -g -pedantic $(DEFS)
 INSTALL = install
